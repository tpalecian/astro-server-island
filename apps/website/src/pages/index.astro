---
import BaseLayout from '@/layouts/base-layout.astro'

import HeroContainer from '@/components/hero-container/index.astro'

const token = import.meta.env.DATOCMS_API_KEY ?? ''
---

<BaseLayout>
	<HeroContainer token={token} />
</BaseLayout>
