query CategoryBySlug($slug: String!) {
  category(filter: { slug: { eq: $slug } }) {
    id
    slug
    filterText
    seoMetaTags: _seoMetaTags {
      attributes
      tag
      content
    }
  }
}
